<%= form_with(model: dogrun, local: true) do |f| %>
  <% if dogrun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dogrun.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% dogrun.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :adress %>
    <%= f.text_field :adress %>
  </div>

  <div class="field">
    <%= f.label :available_time %>
    <%= f.text_field :available_time  %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price  %>
  </div>

  <div class="field">
    <%= f.label :breadth %>
    <%= f.text_field :breadth  %>
  </div>

  <div class="field">
    <%= f.label :other %>
    <%= f.text_field :other  %>
  </div>

  <div class="field">
    <%= f.label :dogrun_image %><br />
    <%= image_tag(@dogrun.dogrun_image.url) if @dogrun.dogrun_image && @dogrun.dogrun_image.url %>
    <%= f.file_field :dogrun_image %>
    <%= f.hidden_field :dogrun_image_cache %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
