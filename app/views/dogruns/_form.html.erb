<%= form_with(model: dogrun, local: true) do |f| %>
  <% if dogrun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dogrun.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% dogrun.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :opening_at %>
    <%= f.text_field :opening_at  %>
  </div>

    <div class="field">
    <%= f.label :closing_at %>
    <%= f.text_field :closing_at  %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price  %>
  </div>

  <div class="field">
    <%= f.label :breadth %>
    <%= f.text_field :breadth  %>
  </div>

  <div class="field">
    <%= f.label :other %>
    <%= f.text_field :other  %>
  </div>

  <div class="field">
    <%= f.label :image %><br />
    <%= image_tag(@dogrun.image.url) if @dogrun.image && @dogrun.image.url %>
    <%= f.file_field :image %>
    <%= f.hidden_field :image_cache %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
