<h2>ドッグラン情報</h2>
<p> <%= image_tag @dogrun.image.url, size: '200x200' %>
</p>
<p>
  <strong>DogrunName:</strong>
  <%= @dogrun.name %>
</p>
<p>
  <strong>address:</strong>
  <%= @dogrun.address %>
</p>
<p>
  <strong>営業時間:</strong>
  <%= @dogrun.opening_at.hour%> :
  <%= @dogrun.opening_at.min %> ~
  <%= @dogrun.closing_at.hour%> :
  <%= @dogrun.closing_at.min%>
</p>
<p>
  <strong>Price:</strong>
  <%= @dogrun.price %>
</p>
<p>
  <strong>breadth:</strong>
  <%= @dogrun.breadth %>
</p>
<p>
  <strong>other:</strong>
  <%= @dogrun.other %>
</p>

<p>
  <%= link_to 'ドッグラン登録者' ,user_path(@dogrun.user) %>
  <%= image_tag  @dogrun.user.image.url %>
<% if log_in? && @dogrun.user_id == current_user.id %>
<%= link_to 'ドッグラン情報編集', edit_dogrun_path(@dogrun) %> 
<% end %>
</p>
<h2>口コミ一覧</h2>
<table>
  <thead>
    <tr>
      <th>title</th>
      <th>content</th>
      <th>image</th>
      <th>口コミ登録者</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
        <% if post.image.url present? %>
        <td><%= image_tag post.image.url %></td>
        <% end %>
        <td><%= link_to '口コミ登録者', user_path(post.user_id) %></td>        
        <td><%= link_to 'Show', post %></td>
        <% if log_in? && post.user_id == current_user.id %>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<P></P>

<% if log_in? %>
<h3>口コミを登録する</h3>
<%= form_with model: @post, local: true  do |f| %>
<%= f.hidden_field :dogrun_id, value: @dogrun.id %>
<%= f.hidden_field :user_id, value: @current_user.id %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= image_tag(@post.image.url) if @post.image && @post.image.url %>
    <%= f.file_field :image %>
    <%= f.hidden_field :image_cache %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>
<% end %>

<%= link_to 'ドッグラン一覧に戻る', dogruns_path %>
